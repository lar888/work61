# ========================================================
# 🌐 Server Configuration
# ========================================================

# Server port and host
PORT=3000
HOST=localhost

# Node environment mode: development | production | test
NODE_ENV=development


# ========================================================
# 🔐 Basic Authentication
# ========================================================

# Admin credentials for accessing /api when Basic Auth is enabled
ADMIN_USER=admin
ADMIN_PASS=12345

# Enable or disable Basic Authentication for /api routes
# true  = Requires "Authorization: Basic ..." header
# false = Public API access (no authentication)
ENABLE_BASIC_AUTH=false
